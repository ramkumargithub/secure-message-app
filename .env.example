# App Port
PORT=3000

# JWT secret key (must be long and random in production)
JWT_SECRET=your_super_secret_jwt_key_here

# Token expiration (e.g., 15m, 1h, 7d)
JWT_EXPIRES_IN=1h

# Database connection string (optional for future persistence)
# DB_URI=mongodb://localhost:27017/secure-messaging

# Log level (info, debug, warn, error)
LOG_LEVEL=info
